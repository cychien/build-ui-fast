import { ComponentDemo } from '../../site/ComponentDemo'

## 圖片背景

針對不同 DPR(1x, 2x), supported format(webp, avif) 及 width，呈現不同尺寸大小的圖片。試著在不同裝置、瀏覽器看這張圖片，會有不一樣的格式與解析度。

<ComponentDemo componentId="background" variantId="ImageBackground" height={400} />

## 圖片背景 (背景尺寸固定)

在小螢幕下，只看得到一部份的背景圖，伸縮視窗看效果。

<ComponentDemo componentId="background" variantId="ImageBackgroundFixed" height={400} />

## 漸層背景

<ComponentDemo componentId="background" variantId="GradientBackground" height={400} />
